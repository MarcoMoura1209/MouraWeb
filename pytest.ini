[pytest]
DJANGO_SETTINGS_MODULE = MouraWeb.settings.local
python_files = test.py tests.py test_*.py tests_*.py *_test.py *_tests.py
addopts =
    --strict-markers
markers = 
    slow: Run test that are slow
    fast: Run fast test